# Git Commits
- Never add "Co-Authored-By" trailers to commit messages.

# General development rules
- Run the related tests when it's a refactor change to make sure the changes don't break the code.
- After each changes, provide a commit meesage using the git-commit-message agent
